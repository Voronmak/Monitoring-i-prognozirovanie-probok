<Window x:Class="Мониторинг_и_прогнозирование_пробок.loginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Мониторинг_и_прогнозирование_пробок"
        mc:Ignorable="d"
        Title="Авторизация в систему" Height="250" Width="350"
        WindowStartupLocation="CenterScreen">

    <StackPanel Margin="15">
        <TextBlock Text="Войдите в систему" FontSize="20" FontStyle="Normal" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#8B2C2C"/>
        <TextBlock Text="Логин:" FontSize="15" FontStyle="Normal" />
        <TextBox x:Name="txtLogin"  Margin="0,5,0,15"/>

        <TextBlock Text="Пароль:" FontSize="15" FontStyle="Normal" />
        <PasswordBox x:Name="txtPassword"  Margin="0,5,0,20"/>

        <Button x:Name="btnLogin" Content="Войти в систему" Click="btnLogin_Click"
                Background="#8B2C2C" Foreground="White" Height="35"/>

        <TextBlock x:Name="txtError" Foreground="DarkRed" Margin="0,10"
                   HorizontalAlignment="Center"/>
    </StackPanel>
</Window>



